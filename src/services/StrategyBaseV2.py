from models.Base import Base
from abc import abstractmethod


class StrategyBaseV2(Base):
	pass
	# def __init__(self):
	# 	super().__init__()

	# @abstractmethod
	# async def process_file(self, image_path: str) -> list[str]:
	# 	pass
